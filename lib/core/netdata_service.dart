// import 'package:egg_network/egg_network.dart';

class NetApi{
  // Future<BResponse<T>> login<T extends BaseJsonResp>(String path, BaseJsonReq req, T resp) async{
  //   BResponse<T> result = await JsonHttpClient().jsonPost(path, req, resp);
  //   return result;
  // }
}